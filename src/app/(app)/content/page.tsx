import { ContentFactoryClient } from "@/components/content/content-factory-client";

export const metadata = {
  title: "Content Factory",
};

export default function ContentPage() {
  return <ContentFactoryClient />;
}
