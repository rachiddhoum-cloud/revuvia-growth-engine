import { SeoIntelligenceClient } from "@/components/seo/seo-intelligence-client";

export const metadata = {
  title: "SEO Intelligence",
};

export default function SeoPage() {
  return <SeoIntelligenceClient />;
}
